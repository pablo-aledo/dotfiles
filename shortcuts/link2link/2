export DISPLAY=:1

subject=$( cat /tmp/link2link_line | sed 's/\(.*\) -> \(.*\)/\1/g' )
link1=$( cat /tmp/link2link_line | sed 's/\(.*\) -> \(.*\)/\2/g' )

echo $subject > /tmp/subject
echo $link1 > /tmp/link1

google-chrome "$link1" >/dev/null 2>/dev/null &
sleep 3

