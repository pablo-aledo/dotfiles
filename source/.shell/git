Git(){
    return
    command=$1
    shift
    case $command in

        (log_graph) \git log --oneline --graph ;;
        (log_follow) \git log --follow $* ;;

        (branch_rewrite) \git rebase -i $(\git merge-base master $(\git rev-parse HEAD)) ;;
        (branch_to_patch) \git diff $(\git merge-base master $(\git rev-parse HEAD)) $(\git rev-parse HEAD) > ../patch.patch ;;
        (branch_create) \git branch $1; git checkout $1 ;;
        (branch_rebase) \git checkout $1; \git rebase $2 ;;

        (push) \git push --set-upstream origin $(\git rev-parse --abbrev-ref HEAD) ;;
        (pull) \git pull --rebase ;;

        (clean) \git clean -df ;;
        (interactive_checkout) \git checkout -p . ;;
        (stash_save_all) \git add .; \git stash push -m "$1"; \git stash apply ;;
        (stash_save_staged) \git add -u; \git stash push -m "$1" ;;
        (remove_last_commit) \git reset --hard HEAD^ ;;
        (unstage) \git reset $* ;;

        (*) \git $command $*;;
    esac
}
